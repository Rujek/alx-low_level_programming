#include <stdio.h>
/**
 * main - prints the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
	 if (c != 'e' && ch != 'q')
	{
	 putchar(c);
	}
	ch++;
	}
	putchar('\n');
	return (0);
}
